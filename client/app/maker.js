const setup = function(csrf) {
    // TODO: Apply CSRF tokens as necessary
};

const getToken = () => {
    sendAjax('GET', '/getToken', null, (result) => {
        setup(result.csrfToken);
    });
};

$(document).ready(function() {
    getToken();
});